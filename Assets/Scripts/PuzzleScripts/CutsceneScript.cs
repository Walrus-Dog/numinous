using UnityEngine;

public class CutsceneScript : MonoBehaviour
{
    TrainMoverScript trainMover;


    private void Start()
    {
        trainMover = GetComponent<TrainMoverScript>();
    }

    // Update is called once per frame
    void Update()
    {
        if (trainMover.)
    }
}
